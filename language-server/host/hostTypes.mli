(** Type that represent an incremental AST that the language parser
    return *)
type ast

(** Type that contains all the informations that result of
    validating a doc (i.e. a list of ast)*)
type valid_doc


(** IM-part *)

(** ?? *)
type task

type state

type query_type

type pp
